<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindItCampus">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LostItemReportActivity"
            android:exported="false"
            android:label="Report Lost Item" />

        <activity
            android:name=".FoundItemReportActivity"
            android:exported="false"
            android:label="Report Found Item" />

        <activity
            android:name=".ReportsListActivity"
            android:exported="false"
            android:label="All Reports" />

        <activity
            android:name=".ItemDetailActivity"
            android:exported="false"
            android:label="Item Details" />

        <activity
            android:name=".OfficeInfoActivity"
            android:exported="false"
            android:label="Lost &amp; Found Office" />

    </application>

</manifest>
